mutation ($input: UploadImageProjectAssetInput!) {
  uploadImageAsset(input: $input) {
    projectAsset {
      project {
        id
      }
      file {
        id
        url
      }
      source
    }
  }
}
